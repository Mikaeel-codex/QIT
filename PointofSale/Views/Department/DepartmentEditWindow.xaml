<Window x:Class="PointofSale.Views.DepartmentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Department"
        Width="520" Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Text="Add Department" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="14">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" VerticalAlignment="Center"/>
                <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Code" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <TextBox x:Name="CodeBox" Grid.Row="1" Grid.Column="1" Margin="0,10,0,0"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Tax Code" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <TextBox x:Name="TaxCodeBox" Grid.Row="2" Grid.Column="1" Margin="0,10,0,0"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Margin %" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <TextBox x:Name="MarginBox" Grid.Row="3" Grid.Column="1" Margin="0,10,0,0"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Markup %" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <TextBox x:Name="MarkupBox" Grid.Row="4" Grid.Column="1" Margin="0,10,0,0"/>

                <CheckBox x:Name="ActiveBox" Grid.Row="5" Grid.Column="1" Content="Active" Margin="0,12,0,0" IsChecked="True"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Style="{StaticResource ModernButton}" Content="Cancel" Width="110" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Style="{StaticResource GoldButton}" Content="Save" Width="110" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
