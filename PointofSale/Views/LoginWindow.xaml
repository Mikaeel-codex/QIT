<Window x:Class="PointofSale.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Width="420" Height="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">

    <Grid Margin="22">
        <Border Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="20">

            <StackPanel>

                <TextBlock Text="Point of Sale"
                           FontSize="26"
                           FontWeight="Bold"
                           Margin="0,6,0,0"/>

                <TextBlock Text="Sign in to continue"
                           Foreground="{StaticResource MutedBrush}"
                           Margin="0,6,0,22"/>

                <TextBlock Text="Username" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
                <TextBox x:Name="UsernameBox" />

                <TextBlock Text="Password" Foreground="{StaticResource MutedBrush}" Margin="0,14,0,6"/>
                <PasswordBox x:Name="PasswordBox" Height="40" Padding="12,8"
                             Background="#2A2A2A" Foreground="White"
                             BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>

                <CheckBox x:Name="RememberMe"
                          Content="Remember me"
                          Foreground="{StaticResource MutedBrush}"
                          Margin="0,12,0,0"/>

                <Button x:Name="LoginBtn"
                        Style="{StaticResource GoldButton}"
                        Height="54"
                        Margin="0,18,0,0"
                        Content="LOGIN"
                        Click="LoginBtn_Click"/>

                <TextBlock x:Name="ErrorText"
                           Foreground="#FF6B6B"
                           Margin="0,14,0,0"
                           TextWrapping="Wrap"
                           Visibility="Collapsed"/>

                <TextBlock Text="(c) PointOfSale"
                           Foreground="{StaticResource MutedBrush}"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"/>


            </StackPanel>
        </Border>
    </Grid>
</Window>