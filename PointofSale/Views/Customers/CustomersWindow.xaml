<Window x:Class="PointofSale.Views.CustomersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customers"
        Width="1200" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="14"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="360"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Customers"
                               FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Add, edit and manage customer accounts"
                               Foreground="{StaticResource MutedBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <TextBox Grid.Column="1"
                         x:Name="SearchBox"
                         Margin="12,0,12,0"
                         VerticalContentAlignment="Center"
                         Style="{StaticResource HintTextBox}"
                         Tag="Search by name, phone or email..."
                         TextChanged="SearchBox_TextChanged"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource ModernButton}" Content="Refresh"
                            Margin="0,0,8,0" Click="RefreshBtn_Click"/>
                    <Button Style="{StaticResource ModernButton}" Content="Add"
                            Margin="0,0,8,0" Click="AddBtn_Click"/>
                    <Button Style="{StaticResource ModernButton}" Content="Edit"
                            Margin="0,0,8,0" Click="EditBtn_Click"/>
                    <Button Style="{StaticResource ModernButton}" Content="Delete"
                            Click="DeleteBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Body -->
        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="CustomersGrid"
                          Grid.Row="0"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="{StaticResource PanelBrush}"
                          Foreground="White"
                          RowBackground="{StaticResource PanelBrush}"
                          AlternatingRowBackground="{StaticResource Panel2Brush}"
                          BorderThickness="0"
                          SelectionMode="Single"
                          ColumnHeaderHeight="34"
                          RowHeight="34"
                          CanUserSortColumns="True"
                          CanUserReorderColumns="False"
                          CanUserResizeRows="False"
                          HorizontalScrollBarVisibility="Hidden"
                          RowDetailsVisibilityMode="VisibleWhenSelected"
                          SelectionChanged="CustomersGrid_SelectionChanged"
                          MouseDoubleClick="CustomersGrid_MouseDoubleClick">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Foreground"                Value="White"/>
                            <Setter Property="Background"                Value="{StaticResource BgBrush}"/>
                            <Setter Property="BorderBrush"               Value="{StaticResource BorderBrush}"/>
                            <Setter Property="BorderThickness"           Value="0,0,0,1"/>
                            <Setter Property="FontWeight"                Value="SemiBold"/>
                            <Setter Property="Padding"                   Value="10,0,10,0"/>
                            <Setter Property="VerticalContentAlignment"  Value="Center"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="Cursor"                    Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Panel2Brush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <!-- Row style — inactive rows greyed out -->
                        <Style TargetType="DataGridRow">
                            <Setter Property="Foreground"      Value="White"/>
                            <Setter Property="FontSize"        Value="13"/>
                            <Setter Property="Height"          Value="34"/>
                            <Setter Property="Background"      Value="{StaticResource PanelBrush}"/>
                            <Setter Property="BorderBrush"     Value="{StaticResource BorderBrush}"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Style.Triggers>

                                <!-- Inactive — greyed out -->
                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                    <Setter Property="Foreground" Value="#555555"/>
                                    <Setter Property="Background" Value="#1A1A1A"/>
                                    <Setter Property="FontStyle"  Value="Italic"/>
                                </DataTrigger>

                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource GoldBrush}"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Panel2Brush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background"      Value="Transparent"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>

                        <Style x:Key="CellText" TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextTrimming"      Value="CharacterEllipsis"/>
                            <Setter Property="Margin"            Value="10,0,10,0"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"
                                            Binding="{Binding Id}"
                                            Width="55"
                                            ElementStyle="{StaticResource CellText}"/>
                        <DataGridTextColumn Header="First Name"
                                            Binding="{Binding FirstName}"
                                            Width="120"
                                            ElementStyle="{StaticResource CellText}"/>
                        <DataGridTextColumn Header="Last Name"
                                            Binding="{Binding LastName}"
                                            Width="120"
                                            ElementStyle="{StaticResource CellText}"/>
                        <DataGridTextColumn Header="Phone"
                                            Binding="{Binding Phone}"
                                            Width="120"
                                            ElementStyle="{StaticResource CellText}"/>
                        <DataGridTextColumn Header="Email"
                                            Binding="{Binding Email}"
                                            Width="*"
                                            ElementStyle="{StaticResource CellText}"/>
                        <DataGridTextColumn Header="City"
                                            Binding="{Binding City}"
                                            Width="110"
                                            ElementStyle="{StaticResource CellText}"/>
                        <DataGridTextColumn Header="Account Bal."
                                            Binding="{Binding AccountBalance, StringFormat=R {0:0.00}}"
                                            Width="110"
                                            ElementStyle="{StaticResource CellText}"/>
                        <DataGridTextColumn Header="Credit Limit"
                                            Binding="{Binding CreditLimit, StringFormat=R {0:0.00}}"
                                            Width="110"
                                            ElementStyle="{StaticResource CellText}"/>

                        <!-- Status column -->
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="6,2"
                                            HorizontalAlignment="Left"
                                            Margin="8,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#1E4D2B"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Background" Value="#3A2A2A"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text"       Value="● Active"/>
                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Text"       Value="● Inactive"/>
                                                            <Setter Property="Foreground" Value="#888888"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                    <!-- Row details — shown when row is selected -->
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <Border Background="#141414"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="0,1,0,0"
                                    Padding="12,8">
                                <StackPanel Orientation="Horizontal">

                                    <Button Content="✏ Edit"
                                            Height="28" MinWidth="80"
                                            Margin="0,0,8,0"
                                            Background="#2F66C8" Foreground="White"
                                            BorderBrush="#1F4E9A" BorderThickness="1"
                                            Cursor="Hand"
                                            Click="EditBtn_Click"/>

                                    <!-- Reactivate — only visible when inactive -->
                                    <Button Height="28" MinWidth="110"
                                            Margin="0,0,8,0"
                                            Background="#2E7D32" Foreground="White"
                                            BorderBrush="#1B5E20" BorderThickness="1"
                                            Cursor="Hand"
                                            Click="ReactivateBtn_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content"     Value="✔ Reactivate"/>
                                                <Setter Property="Visibility"  Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Deactivate — only visible when active -->
                                    <Button Height="28" MinWidth="110"
                                            Margin="0,0,8,0"
                                            Background="#7A3A3A" Foreground="White"
                                            BorderBrush="#5A2020" BorderThickness="1"
                                            Cursor="Hand"
                                            Click="DeactivateBtn_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content"    Value="⊘ Deactivate"/>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>

                </DataGrid>

                <TextBlock Grid.Row="1"
                           x:Name="MsgText"
                           Foreground="{StaticResource MutedBrush}"
                           Margin="2,10,2,0"
                           TextWrapping="Wrap"/>
            </Grid>
        </Border>
    </Grid>
</Window>
